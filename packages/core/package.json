{
  "name": "@interlock/core",
  "version": "0.1.0",
  "private": true,
  "description": "Canonical JSON, hashing, the semantic idempotency key, shared types and Zod schemas.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --build --noEmit"
  },
  "dependencies": {
    "zod": "^4.1.5"
  }
}
